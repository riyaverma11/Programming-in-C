#include <stdio.h>
int main(){
    int userInputFactor;
    int i;
    scanf("%d", &userInputFactor);
    
    for(i=0; i<11; i++){
        printf("%dx%d = %d\n", i, userInputFactor, i*userInputFactor);
    }
    
    return 0;
}
