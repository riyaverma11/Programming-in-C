#include <stdio.h>
int main(void){
    // to print special characters like "" inside a print statement, you use the backslash (\) before the quotes
    printf("Dennis Ritchie said:\n\"The only way to learn a new programming language is by writing programs in it.\"");
    return 0;
}
