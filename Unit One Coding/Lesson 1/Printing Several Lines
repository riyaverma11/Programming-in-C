#include <stdio.h>
int main(void) {
    // to print on separate lines you use the \n; there's no need for a space before it unless we want a space printed in the terminal
    printf("I already know how to:\n");
    printf("- Print text to the screen.\n");
    printf("- Start a new line.\n");
    printf("- Fix errors.\n");
    return 0;
}
